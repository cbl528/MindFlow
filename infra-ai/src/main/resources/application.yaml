# =====================================================================
# MindFlow · infra-ai —— AI 基础设施模块（纯库，无独立可运行入口）
# 本文件的 application.yaml 不会在运行时被加载；
# AI 供应商 / 模型配置（ai.* 节）由业务启动模块 business 扫描加载，
# 统一写在 business/src/main/resources/application.yaml 的 ai: 节中。
# 如需在本地单独调试本模块，可把 ai.* 复制到此处。
# =====================================================================

spring:
  application:
    name: infra-ai
